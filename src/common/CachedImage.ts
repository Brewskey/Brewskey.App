// Stub for image cache flushing
// expo-image handles caching automatically, but we keep this for compatibility
export const flushImageCache = (_url: string): void => {
  // expo-image handles cache invalidation automatically
  // This is a no-op for now
};
